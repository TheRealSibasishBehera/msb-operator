---
title: Sandbox status
description: The observed state of a Sandbox, written by the controller to `.status`. Read-only.
icon: "table-list"
---

{/* Generated by `cargo xtask generate-api-docs`. Do not edit; change the doc-comments on the CRD types and regenerate. */}

The observed state of a Sandbox, written by the controller to `.status`. Read-only.

## SandboxStatus

| Field | Description | Schema |
|---|---|---|
| `appliedCpus` | The `cpus`/`memory` values last confirmed applied to the running guest — either at boot or via a live resize. Diverges from `spec.cpus`/`spec.memory` when an edit is pending a live resize or is `RestartRequired`. | integer |
| `appliedMemory` |  | integer |
| `conditions` |  | [Condition](#condition)[] |
| `exitCode` |  | integer |
| `exposedPorts` | Ports exposed on the per-sandbox Service, reflected so `kubectl describe` shows where a guest listener is reachable. | [ExposedPort](#exposedport)[] |
| `nodeName` |  | string |
| `phase` |  | enum (`Pending`, `Running`, `Succeeded`, `Failed`, `Stopped`) |
| `podName` |  | string |
| `restartCount` | Times the pod has been recreated under `runPolicy: RerunOnFailure`. Drives the requeue backoff and is surfaced so users can see a sandbox is looping. Default `0`. | integer |
| `serviceName` | Name of the per-sandbox ClusterIP Service. Hashed, so not guessable — clients read it here to reach the sandbox's bridge. | string |
| `startedAt` | When the guest reached Running, as a Kubernetes `metav1.Time` (RFC3339). | string |
| `terminatedAt` | When the guest terminated, as a Kubernetes `metav1.Time` (RFC3339). | string |
| `terminationReason` |  | enum (`Completed`, `MaxDurationExceeded`, `IdleTimeout`, `ShutdownRequested`, `Failed`, `OOMKilled`, `Evicted`, `NodeLost`, `Expired`) |

## ExposedPort

| Field | Description | Schema |
|---|---|---|
| `name` _required_ | The Service port name, referenceable by Ingress/Istio. | string |
| `port` _required_ | Port on the Service (and Pod). | integer |
| `protocol` _required_ |  | enum (`TCP`, `UDP`) |

## Condition

| Field | Description | Schema |
|---|---|---|
| `lastTransitionTime` _required_ | lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable. | string |
| `message` _required_ | message is a human readable message indicating details about the transition. This may be an empty string. | string |
| `observedGeneration` | observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance. | integer |
| `reason` _required_ | reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty. | string |
| `status` _required_ | status of the condition, one of True, False, Unknown. | string |
| `type` _required_ | type of condition in CamelCase or in foo.example.com/CamelCase. | string |

