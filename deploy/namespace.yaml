apiVersion: v1
kind: Namespace
metadata:
  name: msb-system
  labels:
    app.kubernetes.io/name: msb-operator
    # The controller runs restricted; sandbox namespaces need baseline (NET_ADMIN).
    pod-security.kubernetes.io/enforce: restricted
