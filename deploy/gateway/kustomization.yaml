apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: msb-system

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: msb-operator

resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/superradcompany/msb-gateway
    newTag: "0.1.0"
