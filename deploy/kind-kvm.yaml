# Mounts the host's /dev/kvm into the node so the daemon's device plugin can
# advertise devices.microsandbox.dev/kvm.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraMounts:
      - hostPath: /dev/kvm
        containerPath: /dev/kvm
