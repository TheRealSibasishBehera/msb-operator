# A real Kubernetes Secret the Sandbox will reference. The plaintext value must
# never appear inside the guest — the guest only ever sees the placeholder.
apiVersion: v1
kind: Secret
metadata:
  name: e2e-sec
type: Opaque
stringData:
  token: SUPERSECRETVALUE123
