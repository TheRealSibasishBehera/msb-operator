apiVersion: sandbox.microsandbox.dev/v1alpha1
kind: Sandbox
metadata:
  name: e2e-maxdur
spec:
  image: "alpine:3.20"
  cpus: 1
  memory: 512
  # Runs for an hour; only maxDurationSecs can stop it in the test window.
  cmd: ["sleep", "3600"]
  runPolicy: Once
  maxDurationSecs: 5
