# Must run first, so the later deletion proves the deadline stopped a live sandbox.
apiVersion: sandbox.microsandbox.dev/v1alpha1
kind: Sandbox
metadata:
  name: e2e-expire-delete
status:
  phase: Running
